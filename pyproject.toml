[project]
name = "community-opinion-dynamics"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "networkx>=3.6.1",
    "numba>=0.65.1",
    "pytest>=9.0.2",
    "scikit-learn>=1.8.0",
]


[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/community_opinions_dynamics", "src/scripts_documents"]
